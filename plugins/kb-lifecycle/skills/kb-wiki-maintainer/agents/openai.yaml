interface:
  display_name: "KB Wiki Maintainer"
  short_description: "Operate a .kb-backed wiki and memory lifecycle"
  brand_color: "#0F766E"
  default_prompt: "Use $kb-wiki-maintainer to ingest a source, answer from the KB, or run lifecycle maintenance."

policy:
  allow_implicit_invocation: true
